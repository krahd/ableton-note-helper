# Temporary deployment mirror: History Through CU

This directory is **not part of Ableton Note Helper**.

It is a temporary static deployment mirror for the private canonical project repository:

`krahd/history-through-cu`

Purpose: expose the CU150 prototype publicly through an already configured GitHub Pages site while the new private project's own Pages setting cannot be enabled through the available GitHub connector.

Canonical source remains:

`krahd/history-through-cu/site/`

Do not edit the prototype here as primary source. Remove this deployment mirror after `krahd/history-through-cu` has its own working public Pages deployment.
